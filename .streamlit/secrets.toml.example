# Copy to secrets.toml locally only — never commit secrets.toml
# On Render: set the same keys as Environment Variables in the dashboard.

DEEPSEEK_API_KEY = "sk-..."
DEEPSEEK_MODEL = "deepseek-chat"
GROQ_API_KEY = ""
LLM_PROVIDER = "deepseek"
