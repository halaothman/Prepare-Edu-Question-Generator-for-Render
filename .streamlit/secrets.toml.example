# Copy to secrets.toml locally only — never commit secrets.toml
# On Render: set DEEPSEEK_API_KEY in Environment Variables in the dashboard.

DEEPSEEK_API_KEY = "sk-..."
DEEPSEEK_MODEL = "deepseek-chat"
LLM_PROVIDER = "deepseek"
